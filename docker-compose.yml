services:
  mongodb:
    image: mongo:8
    container_name: aethermind-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data: