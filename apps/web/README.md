# AetherMind Web

The AetherMind frontend is a React, TypeScript, and Vite application.

## Development

From the repository root:

```bash
pnpm dev
```

The app is served by Vite with hot module replacement enabled.
